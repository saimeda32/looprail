# Changelog

## 0.1.0 (unreleased)

- Core engine: DAG scheduler, verdict router, rails, stall detection,
  journal, resume/replay cache, loopfile parser, lint L001-L006 (Plan 1).
- Adapters: claude-code, codex, aider, copilot-cli, shell, mock; agent
  detection; retry with backoff; infra-error halting (Plan 2).
- CLI: init, run, doctor, lint, status, logs, explain, resume, replay
  (Plan 2).
